# Variables d'environnement du backend Wavedrop
# Copier ce fichier vers .env et remplir avec vos valeurs

# Environnement
NODE_ENV=production
PORT=8080

# Base de données Azure SQL
# Format: sqlserver://USER:PASSWORD@SERVER.database.windows.net:1433;database=DB_NAME;encrypt=true
DATABASE_URL="sqlserver://waveadmin:YOUR_PASSWORD@YOUR_SQL_SERVER.database.windows.net:1433;database=wavedrop-db;encrypt=true"

# JWT Secret (générer un secret fort avec: openssl rand -base64 32)
JWT_SECRET="GENERATE_A_STRONG_RANDOM_SECRET_HERE"

# Azure Storage
# Récupéré depuis: Azure Portal > Storage Account > Access Keys
AZURE_STORAGE_CONNECTION_STRING="DefaultEndpointsProtocol=https;AccountName=YOUR_STORAGE;AccountKey=YOUR_KEY;EndpointSuffix=core.windows.net"
STORAGE_ACCOUNT_NAME="YOUR_STORAGE_ACCOUNT_NAME"

# Frontend URL (pour CORS)
# Development: http://localhost:3000
# Production: https://your-frontend-url.azurewebsites.net
FRONTEND_URL="https://YOUR_FRONTEND_URL"
